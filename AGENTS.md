# EPOCHWRIGHT Agent Guide

本文件是自动化开发代理的唯一默认入口。**任务开始时不要预读整个 `docs/`。** 先用本文件判断任务范围，再按路由逐级加载上下文。节省 token 不能成为猜测规则、跳过契约或降低验证质量的理由。

## 1. 渐进式上下文协议

1. **L0，始终读取**：只读本文件，检查用户请求和将要修改的路径。
2. **L1，确认现状**：新增能力、判断完成度或修改规则时，读 `docs/09-implementation-status.md`；选择下一项工作时再读 `docs/14-todo.md`。纯文档、Git 或明确的局部样式改动可跳过。
3. **L2，按任务路由**：只打开下表列出的文档。长文档先执行 `rg -n "^#{1,3} " <file>` 查看标题，再读取相关章节，不要默认全文加载。
4. **L3，必要时扩展**：只有跨模块、公共契约变更、设计冲突、隐藏耦合或新系统设计时，才读取完整相关文档和 `docs/00-context-guide.md`。
5. **停止扩展**：已经能回答“谁拥有状态、规则语义是什么、边界契约是什么、如何验证”后，不再为背景知识继续加载文档。

需要更详细的路由示例和文档权威层级时，读取 `docs/00-context-guide.md`；范围清晰的普通任务不必读它。

## 2. 任务路由

| 任务 | 最小上下文 | 触发扩展时再读 |
| --- | --- | --- |
| 小型模拟 bug / 纯规则重构 | `10` 相关不变量；`11` 的目标文件小节；相邻测试 | 规则含义不清时读 `02-05` 对应章节 |
| 地图、地形、视线、寻路 | `02` 相关章节；`11 §3`；`12 §3-4` | 改地图 schema 时加 `06 §5-6`、`10 §6` |
| 单位、武器、伤亡、载具 | `03` 相关章节；`12 §5-6` | 涉及 AI 加 `04`；涉及输入/结果加 `06` |
| 情报、AI、外交 | `04` 相关章节；`10 §2/5`；`13 §4-5` | 多势力加 `06 §7`、`12 §8` |
| 冲突、防守、目标、终止 | `05` 对应模式；`12 §7`；相关场景测试 | 改模式契约加 `06 §10/16`、`10 §6` |
| 数据 schema、结果、事件、Worker | `06` 对应契约；`10 §4/6/7`；`11 §4-5` | 破坏性变化时读 `01 §3/6` 并制定迁移 |
| Three.js、视觉效果、镜头 | `07` 对应章节；`11 §6`；`13 §1.3/7` | 新增领域投影时加 `10 §1/7.3`、`06 §14-15` |
| React UI、响应式、观察信息 | `07 §8-12`；`11 §7`；`13 §1.3/7` | 新交互影响玩家权限时加 `01 §2` |
| 测试、CI、构建、发布 | `13` 对应章节；`CONTRIBUTING.md` | 协议/规则测试再读对应领域文档 |
| 选择任务、排期、维护 TODO | `14`；`09`；`08` 的对应阶段 | 新需求或优先级冲突时加 `01` 和领域设计 |
| 纯文档维护 | 目标文档头、`docs/README.md` | 仅在核对事实时读取相关代码/实现状态 |
| 新系统或范围不明确 | `00`、`01`、`09`，再选择领域文档 | 跨边界时读完整 `06`、`10`、`11` 相关部分 |

表中的 `02` 等编号指 `docs/02-*.md`。不要因为一项任务同时出现多个关键词就读取所有文档；按实际会修改的权威状态和边界选择。

## 3. 必须扩大上下文的条件

出现任一情况时，不得继续依靠最小阅读猜测：

- 修改 `BattleSetup`、`BattleResult`、`RenderFrame`、`BattleEvent`、inspection 或 Worker 消息；
- 新增会影响未来 tick 的权威状态；
- 改变 tick 管线顺序、随机键、遍历顺序、终止条件或状态哈希；
- 改动跨越 `sim -> worker -> client -> render/ui` 两个以上边界；
- 代码行为与设计文档不一致，且无法判断是实现缺陷还是设计过期；
- 相关测试失败，显示存在未预期耦合；
- 用户要求完整设计、架构评审或长期扩展方案。

此时读取相关文档的完整章节，必要时全文；token 预算不能优先于正确性。

## 4. 仓库不变量

- `src/sim` 是唯一战斗真相，保持纯 TypeScript，不引用 React、Three.js、DOM、Worker、网络或真实时间。
- 模拟固定 `20Hz`，只有显式 `step()` 推进状态；暂停时战斗计时不变化。
- 模拟禁止 `Math.random()`、`Date.now()`、`performance.now()` 和不稳定决策顺序。随机值由 seed、稳定 ID、tick 和稳定序号推导。
- 新权威状态必须同步初始化、验证、克隆、哈希以及必要的检查/结果投影。
- AI 只能使用本地感知或势力知识，不能读取未发现敌军的真实状态。
- 命中、伤亡、占领和胜负由模拟决定；渲染与 UI 不能反向修改战斗事实。
- 健康与在场状态分轴；当前无复活、投降和友军伤害。
- 玩家当前只能观察、暂停和操作镜头；新增战术命令必须先更新设计和输入契约。
- 新兵种、时代和英雄使用数据、标签或通用能力，不散落名称特判。
- `src/render`、`src/ui` 不得导入 `sim/internal.ts`；`src/client` 不复制战斗规则。

详细解释只在相关任务中读取 `docs/10-engineering-constraints.md`，不要求每次全文读取。

## 5. 工作与验证

- 修改前先搜索目标符号、调用方和相邻测试；优先读代码而不是读取无关设计背景。
- 修复 bug 时先建立能复现问题的回归场景。
- 局部纯规则运行 Vitest；AI、伤害、情报、目标和终止规则增加确定性场景。
- Worker、Canvas、控制或响应式 UI 改动运行 Playwright；3D 内容检查非空与特征像素。
- 跨模块功能、发布前运行 `npm run verify`；普通代码改动至少运行 `npm run check`。
- 性能优化必须对比固定 seed 的状态哈希。
- 不提交 `node_modules/`、`dist/`、`test-results/`、报告、日志或本地环境文件。
- 仅把实际完成并验证的能力写入 `docs/09-implementation-status.md`。

## 6. 文档维护规则

- 每份 `docs/*.md` 顶部必须保留文档头：角色、权威性、何时阅读、可跳过条件和相关代码。
- 新文档必须加入 `docs/README.md` 和 `docs/00-context-guide.md` 的路由；能并入现有文档时不要新增文件。
- 目标设计与当前实现分开：`01-08` 描述设计/路线，`09` 描述已实现事实，`10-13` 描述工程约束与操作，`14` 只保存开放执行项。
- 文档只重复跨任务必须立即可见的不变量；详细内容用链接和章节路由，避免多处维护同一事实。
