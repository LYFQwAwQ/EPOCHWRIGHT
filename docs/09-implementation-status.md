# 当前实现状态

> **文档角色**：当前仓库能力、限制和验证结果的事实清单
> **权威性**：状态说明；若与代码或测试不一致应立即修正
> **何时阅读**：新增功能、判断缺口、发布、写变更说明或引用当前能力时
> **可跳过**：纯 Git 操作、文档导航或已完全定位的局部样式修复
> **相关代码**：全仓库；仅记录已经实现并验证的内容

更新日期：`2026-07-25`

## 已完成：阶段 0 + 阶段 1，以及阶段 2 单目标防守闭环

### 模拟核心

- 纯 TypeScript、固定 `20Hz`、显式 `step()`。
- 相同规则、种子和输入产生相同状态哈希与结果。
- `map-1` 标准地图 schema，使用 TypedArray 分层保存高度、地表类型、水深和地面阻挡标志。
- 地表层可表达草地、沙地、泥地、岩石和铺装；水深层可表达无水、浅水和深水，沼泽由泥地与浅水组合表达。
- 参数化随机高度地图、量化高度、基础山地阻挡，以及草地、沙地、泥地和岩石地表生成。
- 数据表驱动的步行成本矩阵；浅水和沙地减速、泥地浅水组合成本更高、深水不可通行。
- EasyStar 封装的八方向 A*、统一地形成本和禁止切角。
- `BattleSetup` 的 schema/rules 已升级为 `stage-2`；地图使用独立 `map-1` 版本，旧版本输入明确拒绝。
- 两个敌对势力、每组固定 8 人的步枪编组。
- 编组级移动和成员级健康结果。
- 健康状态与在场状态分轴记录。
- 本地目视发现、最后已知位置和同势力 15 tick 情报延迟。
- 弹匣、射击节奏、换弹和无限备用弹药。
- 同时伤害结算、成员受伤、失能和死亡。
- 压制、士气、溃散、撤离、重新集结入口。
- 冲突模式消灭/溃散、僵局和最大时长终止。
- 冲突/防守模式判别输入，冲突模式保持默认并向后兼容。
- 单个防守目标的占领、争夺、恢复、时限与胜负结算。
- 防守方依据高程、移动成本、目标距离和分散度选择阵位。
- 进攻方在没有相关敌情时向目标推进，并以有效在场成员提供占领力。
- 目标状态、占领进度、双方占领力、目标事件和检查接口。

### 网页观察端

- React、TypeScript、Vite、Three.js 和 React Three Fiber。
- 模拟运行在 Web Worker，主线程只接收渲染帧和检查结果。
- 平滑高度地形、程序化岩石和植被。
- 地表、水深和地面阻挡标志驱动的基础地形顶点色；装饰物不会生成在水体中。
- 批量渲染的成员与势力编组标记。
- 根据 `weapon-fired` 事件抽样显示移动曳光和弹着闪光。
- 倾斜正交镜头、平移、旋转、缩放、复位和跟随模式。
- 暂停/继续、新战斗、纯净观察界面。
- 冲突/防守模式切换，每次切换创建独立的新战斗会话。
- 贴合地形的目标区域、边界、占领进度标记和目标状态 HUD。
- 势力概览、编组详情、士气、压制、伤亡、接触和路径信息。
- 页面进入后台时自动暂停。
- 桌面优先、窄屏降级布局。

### 自动验证

- 19 项 Vitest 覆盖地图图层验证、步行成本、深水阻挡、禁止切角、地图克隆与哈希、地图确定性、合法路径、通信延迟、交火换弹、成员减员、防守布阵、推进中射击、占领状态、模式终止、完整战斗复现和终止冻结。
- 6 项 Playwright 覆盖真实 Worker 暂停/继续、镜头缩放、编组检查、模式切换、桌面/窄屏布局、Canvas 像素非空、目标区和射击曳光可见性。
- 生产构建通过。

## 尚未实现

- 多目标组合、防守预备队、增援入口和入口等待。
- 同盟、中立和同盟情报延迟。
- 参数化水体、湿地和道路生成，以及完整静态掩体规则。
- 车辆、乘员、运输、炮兵和空军。
- 英雄与通用能力系统。
- 有限弹药、天气、地形损毁和回放。
- 自动导演与完整势力情报视角。
- 中型和大型规模的正式性能预算验证。

## 下一阶段

继续 [实施路线](./08-delivery-plan.md) 的阶段 2：基于标准地图图层实现参数化组合地形生成和静态掩体，再加入多势力关系、同盟通信、多目标组合、进攻路线选择、增援入口及撤离出口。
